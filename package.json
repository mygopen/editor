{
  "name": "google-docs-blogger-editor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "pages:dev": "npx wrangler pages dev public --compatibility-date=2026-06-15 --compatibility-flag=nodejs_compat",
    "deploy:pages": "npx wrangler pages deploy public --project-name editor --branch main"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "express": "^5.1.0"
  }
}
